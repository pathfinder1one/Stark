# STARK — Requirements
# Core dependency: MuktiVerse (installed from local wheel)
muktiverse==0.1.0

# MuktiVerse transitive deps (already installed, listed for clarity)
httpx>=0.27.0
pydantic>=2.0.0
structlog>=24.0.0
aiofiles>=23.0.0

# Phase 6 — API Server
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Phase 4 — RAG (optional, enable later)
# sentence-transformers>=3.0.0
# chromadb>=0.5.0

# Dev / testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
